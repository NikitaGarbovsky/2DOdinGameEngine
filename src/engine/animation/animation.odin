package animation

LoadEntityAnimations :: proc() {
    sprite_path := "Resources/"

    // #TODO: Merge all the animation files you just downloaded into their own 
    // individual sprite sheets.

    // For each animation
    // Load all animation files into an array
    // Create them as texture handles
    // Store the texture handles somewhere
    // Create Animation data from texture handle.
}